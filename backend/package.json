{
  "name": "ssa-api",
  "version": "1.0.0",
  "description": "Backend API for Siva Suresh Agency — handles all Firestore reads/writes with caching",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "firebase-admin": "^12.0.0",
    "node-cache": "^5.1.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": { "node": ">=18" }
}
