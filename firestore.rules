rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public reads for storefront; writes require any authenticated user (admin panel is login-gated)
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
